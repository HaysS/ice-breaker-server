html
  head
    style
      include normalize.css
    style
      include skeleton.css
  body
    div(class='container')
      div(class='spacer')
      div(class='centered')
        div(class='content')
          h1 Ice Breakerr
          h2(class='prompt') You can see every one of a person's uploaded pictures early by using this form. 
          form(action="/charge", method="post")
            input(type="hidden", name="userUid", value=userUid)
            input(type="hidden", name="profileUid", value=profileUid)
            article
              label=paymentText
            script(
              src="//checkout.stripe.com/v2/checkout.js",
              class="stripe-button",
              data-key=keyPublishable,
              data-locale="auto",
              data-description="Sample Charge",
              data-amount="500")
          